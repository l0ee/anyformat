import { describe, expect, it } from 'vitest';
import { HoverBackground } from './HoverBackground';

describe('HoverBackground Component', () => {
  it('exports HoverBackground component function', () => {
    expect(typeof HoverBackground).toBe('function');
  });
});
